#!/usr/bin/env node
// Arlet end-to-end runner placeholder. Per plan.md, the WebdriverIO/Tauri e2e
// suite is ported after the Milestone 1 UI shell exists. Until then this
// command succeeds as a documented no-op so `test:all` stays green and the
// Phase 0 manual feasibility matrix remains the operative gate.

console.log(
  "[test:e2e] No automated e2e suite yet (Milestone 1). " +
    "Use docs/MUSICKIT_TAURI_FEASIBILITY.md for the manual Phase 0 matrix.",
);
