# MusicKit developer token for local development only.
# Put the real token in `.env.local` at the repository root (gitignored).
# Vite `envDir` is the repo root even though the Vite `root` is `src/`.
# Generate from your Apple Developer Media Services key.
# NEVER ship the .p8 private key in the app, repo, or release artifacts.
# This variable must NOT be set during production builds.
VITE_MUSICKIT_DEVELOPER_TOKEN=

# GPG key ID / fingerprint for signing release artifacts
GPG_KEY_ID=

# GPG passphrase for non-interactive signing on a release VM
GPG_PASSPHRASE=

# Tauri updater signing (from `tauri signer generate`):
TAURI_SIGNING_PRIVATE_KEY=
TAURI_SIGNING_PRIVATE_KEY_PASSWORD=

# Windows Authenticode signing via Azure Artifact Signing (Public Trust)
# Once per Windows release VM (as Administrator), run: npm run setup:win:artifact-signing
# AZURE_CLIENT_SECRET is secret; never commit real credentials.
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_SUBSCRIPTION_ID=
AZURE_CLIENT_SECRET=
AZURE_ARTIFACT_SIGNING_ENDPOINT=
AZURE_ARTIFACT_SIGNING_ACCOUNT=
AZURE_ARTIFACT_SIGNING_PROFILE=
AZURE_ARTIFACT_SIGNING_PUBLISHER=
AZURE_ARTIFACT_SIGNING_PUBLISHER_DN=
# Only the exact value 1 permits an unsigned Windows build; missing or 0 enforces signing.
SKIP_WIN_CODESIGN=0

# Optional: mirror cleaned release/ artifacts to another folder after finalize step.
AFTER_PACK_LOC=

# Override repo owner/name (defaults to BurntToasters/Arlet)
# GH_REPO_OWNER=BurntToasters
# GH_REPO_NAME=Arlet
